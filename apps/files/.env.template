# 복사해서 .env 로 쓰고, .env 는 커밋하지 마.
# 원본 절대경로는 이 파일 정본에 적지 않음.
FILES_VAULT_ROOT=
FILES_VAULT_PASS=
FILES_VAULT_REMOTE=gdrive:karm-files-vault
# 선택. rclone 원격. 공개 img 버킷 금지.
FILES_VAULT_R2=
# 선택. 열람 저장 총량 상한 GB. 넘으면 미러링만 끄고 정본 전송은 계속. 기본 25
FILES_VAULT_R2_MAX_GB=
