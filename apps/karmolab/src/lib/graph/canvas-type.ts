/**
 * lib/graph/canvas-type.ts — **판 위 글자 크기 한 벌** (2026-08-12).
 *
 * 왜: 크기가 9 · 9.5 · 10 · 10.5 · 11 · 13 · 14 로 조각마다 흩어져 있었다. 그러면 같은 급의
 * 글이 자리마다 다른 크기로 나오고, 하나를 키우면 나머지가 어긋난다. 「전체 보기」가 1:1 을
 * 넘지 않게 된 뒤로는 **작은 쪽이 실제로 안 읽혔다**(실측 2026-08-12).
 *
 * 급은 넷이다. 새 글자를 놓을 때는 이 중에서 고른다 — 숫자를 새로 만들지 않는다.
 */
export const TYPE = {
  /** 카드 이름 — 한눈에 읽혀야 하는 것. 굵게 쓴다. */
  title: 13,
  /** 한마디·선 이름표 — 본문 급. */
  body: 11,
  /** 칸 값·작은 설명 — 곁들이는 것. */
  meta: 10,
  /** 뱃지 숫자처럼 **자리만 알리는** 것. */
  micro: 9,
} as const;
