# KarmoGraph — 관계와 생각을 연결하는 그래프 캔버스

관계도·세계관·카드 전개·개념 설명을 한 도구로. **엔진 하나 + 갈아끼우는 어휘 팩**이 설계의 뼈대다.

작업 정본 = `memo/projects/karmolab/tasks/TASK-KL-202-캔버스-범용-관계도.md` (격차 A~AA + 레퍼런스 원장).
앞선 시드 = `TASK-KL-087`.

## 어디에 뭐가 있나

| 파일 | 무엇 |
| --- | --- |
| `karmograph.ts` | 위젯 본체 — 툴바·오른쪽 패널 8종(노드 / 선 / 묶음 / 용어 / 거르기 / 관계망 / 저장 / 도움말)·발표 모드 |
| `packs.ts` | **어휘 팩** 6종(세계관·인물관계도·카드전개·개념설명·구상·조직) + 전 팩 합본 색·아이콘 표 |
| `terms.ts` | 내 용어 — 사람이 직접 만든 노드/관계 종류. 맵이 아니라 **사람**에게 붙는다 |
| `library.ts` | 맵 여러 장 — 목록·현재 맵·복제·삭제. 옛 단일 키에서 자기소멸 이사 |
| `local-storage-adapter.ts` | 한 맵의 읽기·쓰기 (좌표 패치 / 구조 저장 분리) |
| `from-text.ts` | 들여쓴 글 → 노드·선 + 계층 트리 배치 |
| `samples.ts` | 팩별 견본 (빈 화면에서 「예시 넣어 보기」) — 글 문법 그대로 |
| `links.ts` | 설명 속 `[[이름]]` — 가리키는 것 / 나를 가리키는 것 / 이름만 나온 곳 |
| `sna.ts` | 관계망 지표 — degree · betweenness(Brandes) · closeness |
| `tidy.ts` | 가지런히(겹친 것만 밀기 + 격자 스냅) · **나란히 놓기 6방향 · 고르게 벌리기** — 통째 재배치는 일부러 안 한다 |
| `share.ts` | 주소에 그림 담기 (deflate + base64url, 8000자 상한) |
| `storage-health.ts` | 저장 공간 재기 · 위험선 |
| `help.ts` | 할 수 있는 일 + 단축키 목록. **새 기능은 여기 한 줄이 늘어야 한다** |

캔버스 엔진은 여기 없다 — `apps/karmolab/src/lib/graph/` (`canvas.ts` 그리기·조작, `spec.ts` 자료 모양,
`styles.ts` CSS, `adapter.ts` 저장 seam). cockpit 위젯도 같은 엔진을 쓴다.

엔진 쪽에서 **자주 찾게 되는 네 조각** (2026-08-12):

| 파일 | 무엇 |
| --- | --- |
| `canvas-theme.ts` | 판 색을 **앱 테마에서** 읽어 온다(`themeFromCss`). 색을 값으로 박아야 내보낸 SVG 가 혼자 선다 |
| `canvas-type.ts` | 판 위 글자 크기 **네 급**(제목 13 · 본문 11 · 곁들이 10 · 표식 9). 새 숫자를 만들지 말 것 |
| `canvas-raf.ts` | 무거운 다시 그리기를 프레임당 한 번으로 모은다(끌기) |
| `canvas-camera.ts` | `cameraForRect` — 「전체 보기」와 발표 카메라가 **같은 셈**을 쓴다(맞춤은 100% 상한) |

## 저장되는 곳 (localStorage)

| 열쇠 | 무엇 |
| --- | --- |
| `karmograph.index` | 맵 목록 + 지금 열린 맵 |
| `karmograph.map.<id>` | 맵 한 장 (`GraphSpec`) |
| `karmograph.prev.<id>` | 그 맵의 **직전 판** — 사고 났을 때 되살리는 한 판 |
| `karmograph.terms` | 내 용어 (사람 단위, 모든 맵 공용) |

## 검사

```bash
cd apps/karmolab && npm run dev          # 먼저 띄우고
npm run smoke:karmograph                 # 60여 항목 화면 검사
npm run test:karmograph                  # 브라우저 없는 알맹이 시험
```

**CI 에서도 돈다** — `.github/workflows/karmograph-browser-check.yml` (매시 23분 + 손 호출).
배포는 안 막는다. 그 워크플로는 **먼저 앱을 짓고**(`node build.mjs`) 위젯 묶음이 실제로 나올 때까지
기다린 뒤 검사한다 — 개발 서버는 짓는 도중에도 index.html 을 200 으로 준다(2026-08-12 사고).

검사는 **제 서버를 띄우고 판을 얼린다**(`scripts/lib/serve-static.mjs` — 한 번 읽은 파일은 그 판 내내
그때 것을 내준다). 이 작업공간은 세션 여럿이 같은 나무를 고쳐서, 개발 서버를 쓰면 검사 도중 빌드가
돌아 같은 코드가 판마다 다른 답을 냈다(PASS / 빨강 1 / 빨강 6 — 2026-08-12 실측, TASK-KL-243).
도중에 바뀐 파일은 판 끝에 `⚠ 판 도중 옆에서 바뀐 파일 N개` 로 이름이 남는다.
`URL=...` 을 주면 그쪽을 겨눈다(배포된 사이트 확인용).

화면 검사가 **초록이 아니면 새 기능을 얹지 않는다** (2026-08-09 사고 이후 규율 —
위젯이 목록에 등록 안 된 채 20 커밋이 쌓였는데 모든 게이트가 초록이었다).

## 지금까지 온 자리 (2026-08-12 판)

- **처음 30초** (TASK-KL-249, 사람 눈으로 만져 보고 고친 15건): 카드를 만들면 **그 자리에서 바로
  이름을 친다**(빈 상자가 안 남는다) · 「다음 걸음」 한 줄이 선을 이을 때까지 따라온다 ·
  옆 패널은 첫 카드에 7칸(나머지는 「더 보기」, 보기 전용에선 코멘트를 안 접는다) ·
  아이콘에는 이름을 단다(⤢ ▶ ↑ + 와 탭 여덟) · 처음 세 번은 「이 브라우저에 저장됨」 ·
  되돌리기 단추가 **대상**을 말한다 · 판 이름 옆 ✎ · 「보기: 이웃 전체」 ·
  얼굴 없는 카드에 **종류 그림**(색만으로는 안 갈린다).
- 격자: 자리도 크기도 8px, 끌면 **이웃 카드 줄**에 붙고 맞춘 줄이 뜬다(Alt = 자유).

- 화면: 판은 불투명한 바탕 + 앱 테마를 따라간다(밝은 테마 포함). 카드 이름 13px 굵게 / 부제 11px.
  오른쪽 아래 **배율 줄**(− 100% +), 그 위 작은 판은 **화면 밖에 뭔가 있을 때만** 뜬다.
  「⋯」 서랍은 하는 일끼리 6묶음.
- 손: Ctrl+A(보이는 것 전부) · 나란히 놓기 8단추 · 찾기 칸 옆 결과 수(없으면 빨간 「없음」).
- 폰: 빈 판이면 시트를 올려 갈래 고르기를 보여 준다. 카드를 고르면 판이 밀려 **가려지지 않는다**.
- 큰 판: 끌 때 **닿은 선만** 다시 그린다. 실측 600장 87 → 42ms/걸음, 200장 35 → 18ms/걸음.
  남은 병목과 다음 수는 `TASK-KL-234`.
- 말: 사람에게 보이는 이름은 전부 **카드**다(「노드」는 프로그램 말 — 3개 언어에서 걷어냈다).
