/**
 * **push 전에 도는 검사 목록. 한 곳** (2026-08-13)
 *
 * 같은 목록이 두 군데 있었다: `package.json` 의 `verify:prepush` 와
 * `typecheck-pushed.mjs` 의 소스만 읽는 것들. 하나에 검사를 더하고 다른 하나를 잊으면
 * **밀 커밋을 재는 쪽이 조용히 덜 보게 된다**. 이 저장소가 오늘 하루 세 번 당한 그 병이다
 * (게이트 줄만 올림, 목록이 워크플로에만 있음, 라이브 목록 두 벌).
 *
 * 그래서 목록은 여기 하나뿐이고 둘 다 이 파일을 읽는다.
 */

/** push 전 3초 검사. 내 작업 폴더에서 돈다 */
export const PREPUSH = [
  /* 0.5초. 마크다운 렌더러의 user 신뢰가 뚫리면 커뮤니티에 남의 스크립트가 실린다.
     이 모듈엔 후처리 새니타이저가 없다. 이 시험이 곧 방어선이다 (TASK-KL-354). */
  'test:markdown',
  'test:ink',
  'audit:jpegbg',
  'audit:hidden',
  /* 0.05초. 첫 화면 블록이 «그려졌다 사라지는» 것을 막는다. 사람 눈에는 80ms 라 안 보이는데
     사이트 밀림의 대부분이었다(0.103 → 0.0105). 값싸고 잡는 게 크면 앞으로 당긴다. */
  /* 0.05초. 나가는 화면에서 보안 한 줄이 빠지는 것을 막는다. GitHub Pages 는 헤더를
     못 붙여서 이 meta 가 유일한 자리인데, 머리를 조립하는 자리가 여럿이라 조용히 빠진다. */
  'audit:csp-meta',
  /* 0.1초. 인라인 스크립트를 고쳐 놓고 지문을 안 다시 찍으면 **첫 화면이 통째로 죽는다**
     (자물쇠가 그 스크립트를 막는다). 미는 자리에서 잡아야 하는 부류다 (2026-08-17). */
  /* 'audit:csp-shell' 은 자물쇠를 다시 껐으므로 뺐다 (2026-08-17 두 번째). 진짜 이유는
     `gen-csp-shell.mjs` 머리말에 적혀 있다. **지문을 하나라도 적으면 크롬이**
     **`'inline-speculation-rules'` 를 무시한다**(실측). 지문과 미리읽기는 같이 못 산다. */
  /* ★ 3.8초. **껍데기를 고치면 도구 장 145장이 못 찍힐 수 있다**. 그러면 배포가 통째로 선다:
     2026-08-17 에 인라인 손잡이를 걷어냈더니 생성기가 옛 글자를 못 찾아 **배포가 네 판 죽었고**,
     실사이트는 그동안 옛 판을 서빙했다. 미는 자리에서는 아무도 안 보고 있었다. 이제 본다.
     (`CLAUDE.md` 도 셸을 고쳤으면 audit:pages라고 적어 뒀지만, 사람 기억은 게이트가 아니다.) */
  'audit:pages',
  'audit:saylive',
  'audit:iconbtn',
  'audit:aliases',
  'audit:scripts',
  /* ★ 0.4초. **아무도 안 부르는 파일은 깨져도 조용하다** (2026-08-17 실측).
     주석 한 줄을 잘못 닫아 `gen-csp-shell.mjs` 가 통째로 안 읽히게 됐는데, 그 파일을 불러 쓰는
     검사가 하나도 없어 로컬은 전부 초록이었다. **배포 검사가 그것을 실제로 부르는 순간** 터져
     verify 가 세 판 연달아 섰다. 여기 `.mjs` 대부분이 그런 자리다(배포 뒤에만, 한 달에 한 번만).
     돌리지 않고 **읽기만** 한다. */
  'audit:script-syntax',
  'test:tools',
  'test:tool-url',
  'test:i18n:keys',
  'test:karmograph',
  'audit:wf-prereq',
  'audit:orphans',
  /* ★ **3.7초. 검사가 헛빨개지는 병의 유입을 여기서 끊는다** (2026-08-17).
     `waitForTimeout(N)` 뒤에 바로 화면을 읽는 자리는, 바쁜 기계에서 **아직 안 그린 것**을
     읽어 멀쩡한데 빨간불을 낸다. 하루에만 그 부류로 아홉 번 헛고생했고, 지금도 216자리가 남아 있다.
     빚은 톱니로 천천히 갚되 **새로 드는 것은 미는 자리에서 막는다**. 안 그러면 갚는 속도를
     새로 드는 속도가 계속 따라잡는다. 고치는 법은 `lib/settle.mjs`(멎을때까지, 될때까지),
     일부러 재우는 자리는 `재움-의도` 한 줄로 사유를 적으면 안 센다. */
  'audit:sleep-read',
  'audit:i18n-load',
  /* ★ 0.6초. **묶음 파일이 없으면 위젯이 통째로 안 그려진다**. 받기가 실패하면 그 뒤 화면을
     짓는 코드가 아예 안 돌고, 오류도 안 뜬다. 여기 i18n 검사 다섯은 전부 *소스*만 봐서 이 경우를
     아무도 안 잡았고, 새 위젯을 만들 때마다 같은 자리에서 걸렸다 (2026-08-28 보드 위젯이 마지막).
     굽는 일은 이제 `build.mjs` 가 늘 하지만, 이름을 틀리거나 파일을 안 만든 경우는 굽기로 못 잡는다. */
  'audit:i18n-catalog',
  /* ★ 0.7초짜리인데 **배포를 네 판 세운** 병을 잡는다 (2026-08-14): 옮겼다더니 값이 한국어
     그대로. 그 한 열쇠 때문에 verify 가 서고 배포가 통째로 막혔고, 미는 자리에서는 아무도
     안 보고 있었다. 값싸고 잡는 게 크면 앞으로 당긴다. */
  /* 0.2초. 스크립트로 파일을 고치다 역슬래시-b, 역슬래시-n 이 **진짜 글자로** 박히는 사고를 잡는다.
     오늘 실제로 그렇게 정규식 하나가 영영 안 맞았다(`^HlivePage`). `build` 에만 있어서
     미는 자리에서는 아무도 안 봤다. 값이 싸고 잡는 게 확실하면 앞으로 당긴다. */
  'audit:ctl',
  /* 0.05초. 새로 나옴 표와 사이트맵 변경일 기록은 개발 머신에서만 남는데, 안 담고 밀면
     새 도구가 사이트에서 새것으로 안 보이고 변경일 없이 실린다. 미는 자리가 유일한 자리다. */
  'audit:tool-state',
  'audit:spec-locales',
  'audit:memo-derived',
  'audit:generated-registry',
  'audit:shell-t',
  'test:i18n'
];

/**
 * **지어 놓은 것**(`js/`, 말 묶음)을 읽는 검사들. 갓 꺼낸 커밋에는 그게 없다.
 * 밀 커밋을 재는 자리에서 이것들을 돌리면 없는 것을 보고 빨강이 난다(실측 3건).
 */
export const NEEDS_BUILD = ['test:tools', 'test:tool-url'];

/** 밀 커밋을 풀어 놓은 자리에서 돌려도 되는 것들 */
export const SOURCE_ONLY = PREPUSH.filter((g) => !NEEDS_BUILD.includes(g));
