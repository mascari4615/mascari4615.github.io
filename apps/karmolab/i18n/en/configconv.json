{
  "configconv.mdd": "Same settings, different clothes.",
  "configconv.tab": "Config",
  "configconv.label.in": "Paste here",
  "configconv.label.to": "Convert to",
  "configconv.aria.out": "The converted config",
  "configconv.btn.run": "Convert",
  "configconv.btn.swap": "Result to the left",
  "configconv.btn.copy": "Copy",
  "configconv.status.idle": "Paste a config file",
  "configconv.status.ok": "Converted {from} to {to}",
  "configconv.status.same": "Already {kind}. pick a different target",
  "configconv.status.bad": "Could not convert. {msg}",
  "configconv.copy.done": "Copied",
  "configconv.note.limits": "Deliberately not handled: YAML anchors/aliases and multi-document files, TOML date formats. .env and .properties are flat, so nesting is written with dots (a.b)."
}
