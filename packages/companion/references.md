# 레퍼런스 원장 — 뉴로사마까지 남은 것을 밖에서 배운다

> `README.md` 가 **종결 지도**, 이 파일은 그 § 레퍼런스의 살아 있는 판이다.
> 회차별 사연 = `memo/tasks/TASK-KAR-201-*.md`.
>
> **규칙**: 한 줄 감상 금지. 각 항목은 「본 것 → 우리와 뭐가 다른가 → 우리가 뭘 할 수 있나」
> 셋이 다 있어야 남긴다. 셋을 못 채우면 안 적는다(적으면 나중에 아무도 안 읽는다).
> 할 수 있는 것이 나오면 **여기 적고 끝내지 말고** TASK 로 판다.

## 재는 자리 — 뉴로 대조표 (정본은 TASK-KAR-201 § 다음 방향)

| 축 | 뉴로 | 우리 | 갭 |
| --- | --- | --- | --- |
| 실시간 음성 | 스트리밍 STT/TTS + 끊기 | 늘 듣기 + 끊기 + 첫 소리 0.8초 | 없음 |
| 목소리 | 클론 + 노래 모델 | GPT-SoVITS 클론 | 노래 X |
| 기억 | 벡터 RAG, 수년치 자기상 | 3겹 + 뜻 색인 | 자기상 얕음 |
| 도구 | 검색을 상시 씀 | 손 여러 개(읽기 전용) | 조작 X |
| 화면 | 보고 **게임을 직접 함** | 보기만, 품질 얕음 | **조작 X · 인식 얕음** |
| 계통 | 여러 개 동시 | 말줄/일줄 둘, 두뇌는 하나 | 진짜 병렬 X |
| 두뇌 | 2B 로컬 상시 | API | 로컬 상시 X |
| 인격 유지 | 사람이 감시 | 기계 감시 | 우리가 앞섬 |

## 원장

<!-- 새 항목은 맨 위에. 형식:
### YYYY-MM-DD · <이름> · <링크>
- **본 것**:
- **우리와 다른 점**:
- **할 수 있는 것**: (→ TASK-XXX 로 팠으면 번호)
-->

### 2026-08-21 · 「자막 제공 및 광고를 포함하고 있습니다」에는 이름이 있다

- **본 것**: 받아쓰기 쪽 2026 지형을 보다 우리 증상의 이름을 찾았다 — **whisper 는 조용할 때
  지어낸다**(silence hallucination). 자막 데이터로 배워서 무음 구간에 자막 상투구를 뱉는
  것이고, 스트리밍을 원래 못 한다는 것도 같이 꼽힌다. 대안 숫자: **Voxtral Transcribe 2**
  (Mistral, 2026-02, **4B · Apache 2.0 · 스트리밍 native**) FLEURS 평균 **WER 5.9%** vs
  whisper large-v3 **7.4%** — 다만 **13개 언어만**. 속도는 **NVIDIA Parakeet TDT 1.1B** 가
  RTFx 2,000 대. 말 끝 판정을 **모델 안에 넣은** 것도 나왔다(Deepgram Flux — 바깥 VAD 를
  안 쓴다). 실시간 최상단은 AssemblyAI Universal-3.5 Pro Realtime — WER **3.40%** ·
  최종 조각까지 **p50 약 110ms**.
  [열린 STT 벤치 2026](https://northflank.com/blog/best-open-source-speech-to-text-stt-model-in-2026-benchmarks) ·
  [Voxtral vs Whisper](https://weesperneonflow.ai/en/blog/2026-03-31-voxtral-whisper-open-source-speech-models-comparison-2026/) ·
  [whisper 대안 정리](https://www.assemblyai.com/blog/whisper-alternatives)
- **우리와 다른 점**: 우리는 whisper 를 쓴다(`sense/whisper.ts`). 그래서 기억에
  **「자막 제공 및 자막 제공 및 광고를 포함하고 있습니다.」가 5건** 남아 있다(107회차 실측).
  대조표의 「실시간 음성 = 갭 없음」은 기능 얘기고, **환청을 거르는 자리는 없다.**
  61회차에 「깨진 글은 기억이 되면 안 된다」를 세웠는데 그건 **깨진 글자**를 보는 것이라
  멀쩡한 문장 모양의 환청은 그대로 지나간다.
- **할 수 있는 것**: 모델을 바꾸는 건 사용자 영역(디스크·시간)이지만, **거르는 것은 지금
  우리 코드로 된다.** 받아쓰기 결과가 사람 말로 담기기 **전에** 자막 상투구를 걷어낸다 —
  61회차 지킴이 옆자리다. 111회차에 배운 대로 사전을 두 벌 만들지 말고 한 자리에.
  → 판 TASK: **TASK-KAR-240**

### 2026-08-21 · 상주시킬 만한 작은 머리 — 숫자로 고른다

- **본 것**: 로컬 소형(1B~4B)의 2026 지형. **Qwen 3.5 4B** = 내려받기 **3.4GB**, 맥락 **256K**,
  **그림도 입력으로 받는다**. **Gemma 3** 는 270m·1b·4b·12b·27b 로 나오고 **4B 이상이 다중 모드**다.
  속도는 갈래가 뚜렷하다 — 2B 미만이 **350 tok/s** 대, **3~4B 가 180~290 tok/s**.
  VRAM 기준선: 8GB 면 9B 급도 돌고, **6GB 이하면 4B 이하**가 여유롭다.
  대화 결만 놓고 보면 4B 쪽이 더 작은 것들보다 확실히 낫다.
  [작은 모델 비교 2026](https://tinyweights.dev/posts/best-small-language-models-2026/) ·
  [SLM 순위 1B~14B](https://localaimaster.com/blog/small-language-models-guide-2026) ·
  [Ollama 모델 정리](https://computingforgeeks.com/ollama-models-cheat-sheet/)
- **우리와 다른 점**: 대조표의 「두뇌 = 로컬 상시 X」에 이제 **고를 것과 값**이 붙는다.
  우리 첫 낱말은 **9~14초**고 그중 바닥이 CLI 왕복 10.7초다(102회차). 4B 를 상주시키면
  그 바닥이 사라진다. 더 맞물리는 것 하나 — **4B 급이 그림을 입력으로 받는다.** 우리는
  99·100·104회차에 화면을 그림 + 글자로 두뇌 앞에 놓는 길을 이미 깔아 뒀다. 로컬로 옮겨도
  그 재료가 그대로 쓰인다.
- **할 수 있는 것**: **TASK-KAR-237** 의 「사용자 결정 자리」에 이 숫자를 그대로 올린다 —
  받는 것(3.4GB) · 기대 속도(180~290 tok/s) · VRAM 기준(6GB 이하면 4B). 우리 쪽 코드는
  이미 자리가 있다(`brain/assistant.ts` → `karmolab-ai` provider 라우터에 `ollama` 포함).
  103회차에 그 두뇌가 키가 없어 침묵하던 것을 살리는 것이 첫 걸음이고, 그 다음은 재기다
  (5회 평균 TTFT 를 CLI 10.7초와 나란히).

### 2026-08-21 · 「손을 안 쓴다」는 우리만의 병이 아니다 (그리고 고치는 자리가 정해져 있다)

- **본 것**: 손(도구)을 부르는 신뢰도는 2026 에도 **모델 능력만으로 안 된다**는 게 결론이다.
  작거나 지시 정렬이 덜 된 모델에는 **제약 디코딩**(모델이 스키마에 맞는 토큰만 낼 수 있게
  강제)을 권한다. 값도 있다 — 스키마마다 유한 상태 기계를 컴파일해야 하고, xgrammar 는 그걸
  **스키마당 한 번 20~50ms** 로 밀어 놨다. 다만 공짜가 아니다: **「제약세(constraint tax)」**
  라는 이름의 연구가 나왔다 — 출력 형식을 조이면 **형식은 맞는데 내용이 나빠지는** 맞바꿈이
  작은 모델에서 관측된다. 그리고 평가의 초점이 「부를 수 있나」에서 **「여러 turn 에 걸쳐,
  애매한 상황에서, 인자까지 제대로 부르나」**로 옮겨갔다.
  [도구 호출 표준·벤치 정리](https://zylos.ai/research/2026-04-07-tool-use-function-calling-standards-benchmarks/) ·
  [운영 아키텍처 패턴](https://zylos.ai/research/2026-04-16-tool-augmented-llm-agents-production-architecture/) ·
  [제약세 연구](https://arxiv.org/pdf/2606.25605)
- **우리와 다른 점**: 우리 손은 **말 속의 글자 표**(`[[누르기: 3]]`)로 불린다 — 제약이 전혀
  없는 방식이다. 그래서 42회차에 **0/10**, 122회차에도 「누를게」라고 말만 하고 안 눌렀다.
  우리가 고른 우회는 **우리가 미리 써 주기**(recall·hands-auto)였는데, 누르기처럼 **위험해서
  미리 못 쓰는 손**에는 그 우회가 안 먹는다. 즉 우리 갭은 「모델이 약하다」가 아니라
  **부르는 통로에 아무 강제가 없다**는 것이다.
- **할 수 있는 것**: 두 갈래가 있고 지금 당장 되는 건 뒤엣것이다. ① 제약 디코딩 —
  두뇌가 CLI 라 우리 손 밖이다(로컬 상주로 가면 열린다, TASK-KAR-237). ② **뒤에서 잡기** —
  122회차에 넣은 `promisedButSkipped` 가 그 자리다. 「하겠다」고 말했는데 표가 없으면 입 앞에서
  다시 시킨다. 밖의 표현으로 하면 **강제가 없으면 검증으로 메운다.** 다음 칸은 그걸
  **누르기 말고 다른 손에도** 넓히는 것 — 다만 41회차 교훈대로 「진짜 약속」은 살려 둔 채로.

### 2026-08-21 · 뉴로가 게임을 하는 방식 — 화면이 아니라 **게임이 스스로 말한다**

- **본 것**: 뉴로가 게임을 하는 길은 **화면 조작이 아니다.** 게임 쪽이 웹소켓으로 붙어서
  ① **지금 무슨 일인지 평문으로 보내고**(Context), ② 할 수 있는 짓을 **이름 + 설명 +
  JSON 스키마**로 등록하고(Registered actions), ③ 필요하면 「이 중에서 골라라」로 **강제**한다
  (Forced actions). 모델은 **행동 이름 + JSON** 만 돌려주고, **검사와 실행은 게임 쪽 몫**이다.
  그래서 모델 출력이 단순·타입 있음·검사하기 쉬움으로 유지된다. Unity·Godot 공식 SDK 가 있고
  다른 엔진은 프로토콜을 직접 구현한다. **턴제 게임에 잘 맞고**(Inscryption, Buckshot Roulette),
  실시간 게임은 행동을 높은 수준으로 추상화해야 한다.
  [neuro-sdk](https://github.com/VedalAI/neuro-sdk) ·
  [Unity SDK 예제](https://deepwiki.com/VedalAI/neuro-game-sdk/3.4-unity-sdk-examples) ·
  [게임 API 설계 해설](https://nikechan.com/dev-blog/neuro-sama-game-api-design-en)
- **우리와 다른 점**: 우리는 121회차에 **화면을 눌러서** 만지는 길을 냈다 — 창을 읽고
  번호를 붙이고 Invoke 한다. 저쪽은 그 길을 아예 안 쓴다. **앱이 협조하면 화면을 안 봐도
  된다**는 것이 그쪽 답이다. 우리 쪽에 그 협조자가 하나 있다 — **WM(유니티 게임)**.
  같은 작업공간 안에 있고 Unity SDK 가 공식으로 있다.
- **할 수 있는 것**: 화면 조작은 「남의 앱」용으로 두고, **우리 게임에는 협조 통로**를 낸다 —
  WM 이 「지금 무슨 판인지」를 평문으로 보내고, 할 수 있는 짓을 스키마로 등록하면, 동반자가
  **화면을 안 보고도** 같이 논다. 손 얼개(`hands/from-file.ts`)는 이미 파일 하나 = 손 하나라
  스키마 등록과 결이 같다. 지금 판단: **아직 TASK 로 안 판다** — WM 쪽 손을 대는 일이고
  그건 사용자 영역(게임 설계)에 걸린다. 숫자와 길만 여기 남긴다.

### 2026-08-21 · 무엇을 누를지 고르게 하려면 **번호를 붙여 준다**

- **본 것**: 화면을 만지는 에이전트가 「무엇을 누를지」 고르는 표준 방식이 **Set-of-Mark**다 —
  찍은 그림 위에 **번호·글자표를 얹고**, 모델은 좌표가 아니라 **그 표를 고른다**. 왜 이렇게
  하냐면, 날 픽셀만 주면 모델이 **어디인지 찾는 일과 무엇을 할지 생각하는 일을 동시에** 해야
  해서 주의가 흩어지기 때문이다. 표를 얹으면 그 둘이 갈라진다. 값도 재 봤다 — 그림 질문
  정확도 **8~15%** 향상, 미세한 자리 짚기는 모델을 다시 훈련하지 않고도 크게 좋아진다.
  다만 **표 모양에 민감하다** — 색·번호 위치·크기만 바꿔도 성적이 눈에 띄게 흔들린다.
  [범용 시각 그라운딩](https://arxiv.org/html/2410.05243v1) ·
  [표 기반 시각 프롬프팅 정리](https://www.emergentmind.com/topics/mark-based-visual-prompting) ·
  [GUI 에이전트 진단](https://arxiv.org/html/2604.04399)
- **우리와 다른 점**: 우리는 104·117회차에 창 안을 **글자 목록**으로 준다 —
  「Button 「저장」 (10,20,60,30) — 할 수 있는 것: Invoke」. 그림 위에 표를 얹지는 않는다.
  대신 **목록에 번호가 없다.** 두뇌가 「저장 눌러」라고 말해도 우리는 그걸 **이름으로 다시
  찾아야** 하고, 같은 이름이 둘이면(탭 닫기 단추가 넷이다 — 117회차 실측) **어느 것인지 모른다.**
  Set-of-Mark 가 푸는 문제를 우리는 아직 안 풀었다.
- **할 수 있는 것**: 그림에 표를 그리는 건 나중 일이고, **목록에 번호를 붙이는 것은 지금 된다** —
  `assets/capture-screen.ps1` 이 `TREE=` 를 만들 때 `i` 칸을 더하고, 두뇌가 보는 글에
  「[3] Button 「저장」」처럼 실으면 된다. 그러면 누르기 손이 **번호 하나**만 받는다
  (이름으로 다시 찾는 자리가 사라진다 = 같은 이름 여럿 문제도 같이 사라진다).
  → **TASK-KAR-241** 2단계에 넣는다(새 TASK 안 판다).

### 2026-08-21 · 먼저 말 거는 건 **적당히**가 최고점이다 (많아도 적어도 깎인다)

- **본 것**: 먼저 말 거는 것(proactive)을 실제로 재 본 연구들이 있다. **중간 빈도가 가장 높게
  평가되고 양극단은 둘 다 깎인다** — 많이 걸어도, 안 걸어도 나쁘다. 사람들이 꼽은 짜증의 결도
  구체적이다: ① **기계적인 루틴 공유**(참가자 5명 — 매일 아침 7시에 「나 오늘 뭐 할 거야」),
  ② **상대가 안 좋아하는 화제를 계속 들이미는 것**(4명 — 농구 싫다는데 계속 농구 하자고),
  ③ **남이 옆에 있을 때 개인적인 얘기**를 꺼내는 것. 즉 나쁜 건 「먼저 말 거는 것」 자체가
  아니라 **빈도와 자리를 안 보는 것**이다.
  [로봇의 선제 발화 빈도 연구](https://www.nature.com/articles/s41598-026-49412-3) ·
  [ComPeer: 선제적 또래 지지 에이전트](https://arxiv.org/pdf/2407.18064) ·
  [언제·어떻게 말 걸까(그리고 걸지 말까)](https://arxiv.org/pdf/2005.01322)
- **우리와 다른 점**: 우리 「먼저 말 걸기」(`nudge`)는 **조용한 시간·창이 바뀐 것**으로 건다 —
  ②·③ 은 피해 가지만 ①(기계적)에 가깝다. 결정적으로 **건 뒤에 어땠는지를 안 본다.**
  창 제목이 바뀌면 걸고, 조수님이 무시했는지 받아줬는지는 다음 판단에 안 들어간다.
  대조표의 「먼저 말하기」는 기능으로는 있지만 **적당한지 재는 자리가 없다.**
- **할 수 있는 것**: 재는 재료는 이미 다 있다 — `what-landed.ts`(방금 한 말이 착지했나) ·
  `heart`(웃어줌/무시당함) · 발동 기록(`tally`). 먼저 건 turn 의 **착지율**을 세어 자주
  무시당하면 **간격을 늘리고**, 받아주면 줄인다. 「몇 분마다」를 손으로 정하지 말고 **반응이
  정하게** 한다 — 113회차에 「거르기만 하고 세지 않으면 도는지 모른다」로 배운 것과 같은 결이다.
  → 판 TASK: **TASK-KAR-242**

### 2026-08-21 · 누를 때는 **좌표가 아니라 그 단추한테 말한다**

- **본 것**: 윈도우에서 무언가를 누르는 길이 둘이다. ① **SendInput** — 마우스·키보드 흐름에
  가짜 입력을 끼워 넣는다. 화면 좌표가 필요하고, 다른 스레드가 입력을 막고 있으면 **0을
  돌려주며 그냥 실패**한다. Node 쪽 바인딩은 있지만 아직 **키 조합에 버그가 남은 「작업 중」**
  이다. ② **UI Automation 의 Invoke 패턴** — 마우스 포인터를 안 쓰고 **컨트롤이 스스로 내놓은
  동작을 부른다**(윈도우 내부 메시지). 그래서 **창 위치·DPI 배율이 달라도 그대로 먹고**,
  대개 더 빠르다. Invoke 말고도 Toggle·Select·Expand 같은 패턴이 각각 있다.
  [Invoke 패턴 문서](https://learn.microsoft.com/en-us/dotnet/framework/ui-automation/invoke-a-control-using-ui-automation) ·
  [패턴 개요](https://learn.microsoft.com/en-us/windows/apps/dev-tools/winapp-cli/ui-automation) ·
  [SendInput 문서](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-sendinput)
- **우리와 다른 점**: 대조표의 「화면 = 조작 X」가 아직 그대로다. 다만 104회차에 UI Automation
  으로 창 안을 읽으면서 **요소마다 좌표까지** 받아 뒀는데, 그 좌표를 쓰려면 ①번 길이 된다 —
  DPI 에 휘둘리고(99회차에 정확히 그 문제로 화면을 3분의 1만 찍고 있었다) 조용히 실패한다.
  **이미 트리를 들고 있으므로 ②번 길이 우리에게 더 가깝다.** 우리가 이미 지나온 자리라
  새로 배울 것도 적다.
  **단, 118회차 실측으로 이 항목을 한 번 깎았다 — Invoke 는 앱에 따라 아예 없다.**
  Windows Terminal 은 「탭 닫기 | Invoke」를 그대로 내놓는데, WinForms 창의 단추는
  `GetSupportedPatterns` 가 **빈 목록**이다(레거시 브리지). 그러니 ②번을 기본으로 삼되
  **폴백**(LegacyIAccessible 의 기본 동작 → 그것도 없으면 좌표)이 있어야 한다.
- **할 수 있는 것**: 누르기를 붙일 때 `assets/capture-screen.ps1` 이 쓰는 그 트리에서
  **요소를 집어 Invoke** 한다 — 좌표로 클릭하지 않는다. `TREE=` 줄에 지금은 이름·갈래·자리만
  싣는데, **무슨 동작을 지원하는지**(Invoke/Toggle/…)를 한 칸 더 실으면 「누를 수 있는 것」이
  두뇌 눈에 바로 보인다. 그리고 그 손은 되돌릴 수 없으니 **TASK-KAR-239 관문**을 그대로 탄다.
  → 판 TASK: **TASK-KAR-241**

### 2026-08-21 · 조작을 붙이기 **전에** 세우는 것 — 되돌릴 수 있나

- **본 것**: 화면을 만지는 에이전트에서 2026 의 표준은 「똑똑하게 판단하기」가 아니라
  **규칙으로 잠그기**다. 확인 자리를 두 겹으로 나눈다 — 모델이 스스로 올리는 것(escalation)과
  **모델 상태와 무관하게 규칙이 세우는 관문**(rule-triggered gate). 관문에 거는 기준은 하나다:
  **되돌릴 수 있나.** 되돌릴 수 없는 쓰기(지우기·보내기·결제), 자격 넘기기, 규제 데이터는
  모델이 뭐라 하든 사람 확인. 읽기·찾기처럼 되돌릴 수 있는 것은 안 묻는다(다 물으면 아무도
  안 읽는다). 하는 일은 **전부 자국으로 남긴다.** 실행 자리는 샌드박스가 기본이다 — Anthropic
  computer use 는 도커 안에서 돌라고 못 박고, Gemini 쪽은 행동마다 안전 판정을 붙여 MCP 의
  사람 승인 자리로 넘긴다(MCP 모드에서는 기본 켜짐).
  [안전 배치 정리](https://toloka.ai/blog/computer-use-agents-what-they-are-how-they-work-and-how-to-deploy-them-safely/) ·
  [사람 확인 자리 설계](https://www.permit.io/blog/human-in-the-loop-for-ai-agents-best-practices-frameworks-use-cases-and-demo) ·
  [기업 배치 지침](https://www.digitalapplied.com/blog/agent-computer-use-enterprise-automation-playbook)
- **우리와 다른 점**: 우리 손 열두 개는 **거의 다 읽기**다(찾아보기·할일보기·창목록·파일정보).
  쓰는 것은 적어두기·궁금해하기·알려주기 정도고 전부 우리 파일 안이다. 그래서 여태 이 관문이
  없어도 사고가 안 났다. 그런데 104회차에 화면 요소의 **좌표**가 생겼다 — 다음 칸이 누르기다.
  게다가 우리는 샌드박스가 아니라 **조수님 화면 그대로** 돈다. 저쪽이 도커를 깔고 하는 일을
  우리는 맨바닥에서 하게 된다. 손 명세(`hands/from-file.ts`)에는 「언제 쓸지」는 있어도
  **「되돌릴 수 있나」가 없다.**
- **할 수 있는 것**: 조작을 붙이기 **전에** 손 명세에 `되돌릴 수 있나` 한 칸을 넣고, 코어가
  되돌릴 수 없는 손은 **사람 확인 없이는 못 쓰게** 한다(42회차 교훈대로 두뇌 판단에 안 맡기고
  규칙으로). 자국은 이미 `발동-기록.json` 이 남기고 있으니 그 위에 얹으면 된다.
  → 판 TASK: **TASK-KAR-239**

### 2026-08-21 · 같은 걸 만드는 둘 — 하나는 기억을 남한테 맡기고, 하나는 게임을 한다

- **본 것**: 우리와 같은 것을 만드는 열린 물건 둘을 실제로 봤다(전엔 이름만 적어 뒀다).
  **Open-LLM-VTuber** — ASR·LLM·TTS·Live2D/VRM·말 끊기·눈·대화 기록을 부품으로 갈아끼우는
  얼개고, **오래 가는 기억을 Letta 에 맡긴다**(제 손으로 안 짓는다. 그래서 지난 대화를
  기억한다). 클라우드 없이 전부 로컬로도 돈다. **AIRI** — 뉴로사마에서 출발했고, `xsAI` 라는
  한 겹으로 **LLM 제공자 40여 개**를 갈아끼운다. 처음부터 웹 기술(WebGPU·WebAudio·Web
  Workers·WebAssembly)로 지어서 브라우저·폰(PWA)에서도 돈다. 그리고 **마인크래프트를 실제로
  한다**(mineflayer).
  [AIRI 구조](https://deepwiki.com/moeru-ai/airi) ·
  [Open-LLM-VTuber](https://github.com/Open-LLM-VTuber/Open-LLM-VTuber) ·
  [AI VTuber 목록](https://github.com/proj-airi/awesome-ai-vtubers)
- **우리와 다른 점**: ① **기억** — 저쪽은 기억을 남의 물건(Letta)에 맡기고 우리는 직접 짓는다.
  105회차에 「아는 것」이 통째로 오염돼 있던 걸 보면 직접 짓는 값을 우리가 치르고 있다.
  다만 우리 뜻 색인·사건·되새김은 그쪽에 없는 결이라 통째로 갈아탈 일은 아니다.
  ② **행동** — AIRI 는 게임을 **API 로** 한다(mineflayer). 우리는 화면을 눈으로 읽고 조작은
  아직 없다(104회차에 좌표만 갖췄다). 화면을 통째로 다루는 것보다 **게임 하나를 API 로 하는
  쪽이 훨씬 값싼 첫 걸음**이라는 게 저쪽이 보여 주는 것이다.
  ③ **몸** — 우리 창은 Tauri exe 고 저쪽은 브라우저다. 우리 쪽이 무겁지만 상주창·화면 읽기는
  브라우저로 원리상 안 된다(`web.ts` 에 실패 기록). 이건 우리가 이미 값을 치르고 고른 것이다.
- **할 수 있는 것**: ① 기억 2단계(TASK-KAR-236)를 설계할 때 **Letta 식으로 — 기억을 글 한 덩이가
  아니라 갈아끼울 수 있는 상태 블록으로** 둔다. 남의 물건을 들이지 않아도 그 모양은 베낄 수 있다.
  ② 조작의 첫 걸음을 **화면이 아니라 게임 하나**로 잡는다 — 뉴로 대조표의 「화면 = 조작 X」를
  깨는 가장 싼 길이고, 손 얼개(`hands/from-file.ts`)에 그대로 얹힌다.

### 2026-08-21 · 말을 주고받는 틈에는 **정해진 숫자**가 있다

- **본 것**: 2026 의 음성 에이전트는 틈을 ms 로 잰다. **말 끊기(barge-in)** = 사람이 말을 멈춘
  뒤 소리를 끊기까지 **150ms 미만**. **턴 간격** = 내 말이 끝나고 상대 첫 소리까지 **200~450ms**.
  설계 목표는 **첫 소리 300ms**(흐름을 겹쳐서 낸다). 조각별로도 갈라 잰다 — 두뇌 첫 토큰
  250~300ms, TTS 첫 소리 40~90ms(Cartesia Sonic 4 ~40ms · Deepgram Aura-2 ~90ms).
  통짜 speech-to-speech 는 0.8~3초 대. **말이 끝났는지**를 VAD 대신 작은 트랜스포머로 보면
  **말 중간에 끊는 사고가 87% 줄고 늦어지는 건 20ms** 뿐이다. 그리고 STT **앞에** 「나한테 한
  말인가」를 가리는 자리를 두어 제 TTS 메아리·옆사람 말로 깨어나는 것을 막는다.
  [실시간 vs 턴제 얼개·수치](https://softcery.com/lab/ai-voice-agents-real-time-vs-turn-based-tts-stt-architecture) ·
  [끊기·턴 잡기 2026](https://futureagi.com/blog/voice-ai-barge-in-turn-taking-2026/) ·
  [실시간 API 비교](https://inworld.ai/resources/best-realtime-apis-for-voice-ai)
- **우리와 다른 점**: 대조표는 「실시간 음성 = 갭 없음」이라 적혀 있지만 그건 **기능**이
  있다는 뜻이지 숫자를 맞췄다는 뜻이 아니다. 우리가 아는 값은 첫 소리 **1.8초**(59회차) 하나뿐이고,
  **말 끊기 지연은 한 번도 안 재 봤다**(76회차에 기능만 넣었다). 말이 끝났는지도 소리 크기
  문턱으로만 본다 — 「말 중간에 끊긴다」가 나면 문턱을 손으로 만지는 수밖에 없다.
  「나한테 한 말인가」를 가리는 자리는 아예 없다(98회차에 마이크가 안 잡히던 것과 반대 방향의 위험).
- **할 수 있는 것**: `scripts/measure-lanes.mjs` 옆에 **틈을 재는 자리**를 하나 더 만든다 —
  ① 사람이 멈춘 뒤 소리가 끊기기까지 ② 사람 말 끝 → 첫 소리. `sense/whisper.ts` 와
  `voice/*` 가 이미 시각을 알고 있으니 새로 만들 것은 재는 자리와 기준선이다.
  기준선이 서면 「말 중간에 끊는다」·「늦다」를 느낌이 아니라 숫자로 말할 수 있다. → **TASK-KAR-238**

### 2026-08-21 · 뉴로사마 근황 — 지연 1~3초, 손은 「스키마 + 강제 선택」

- **본 것**: 공식 `VedalAI/neuro-sdk` 는 범용 에이전트 틀이 아니라 **웹소켓 규약**이고, 뼈대가
  셋뿐이다 — **Context**(게임이 지금 무슨 일인지 평문으로 보낸다) · **Registered actions**
  (게임이 할 수 있는 짓의 이름·설명·**JSON 스키마**를 선언한다) · **Forced actions**(게임이
  「이 중에서 골라라」라고 **강제**한다. 지금 상태와 물음을 같이 줄 수 있다). 방송이라
  **응답 1~3초**를 원 능력보다 위에 둔다. 오래 가는 자기상은 벡터 DB 로 몇 년치를 이어 간다.
  기계도 늘렸다 — 2026-05 새 서버: 28TB · RTX Pro 6000 Blackwell.
  [neuro-sdk 정리](https://lin-guanguo.github.io/llm-memory-research/neuro-sama.research/) ·
  [VedalAI](https://github.com/vedalai) ·
  [주간 기록 2026-05-25](https://blog.neurosama.com/2026/05/25/weekly-update)
- **우리와 다른 점**: ① **지연** — 저쪽 목표가 1~3초인데 우리는 첫 낱말 **9~14초**다(102회차
  실측). 같은 급이라고 말할 수 없는 유일하게 분명한 축이다. ② **손을 부르는 방식** — 우리는
  두뇌가 말 속에 `[[이름: 인자]]` 라는 **글자 표**를 적어야 손이 돈다. 42회차에 그게 0/10 로
  실패해서 「우리가 미리 써 주기」로 우회했다. 저쪽은 **스키마로 선언하고 필요하면 고르기를
  강제**한다 — 우회가 아니라 정공법이다.
- **할 수 있는 것**: 손 명세(`hands/from-file.ts`, 파일 하나 = 손 하나)에 **인자 스키마**를
  같이 적게 하고(44회차에 「언제 쓸지」를 붙인 그 자리), 코어에 **「이번엔 반드시 하나 골라라」**
  모드를 낸다. 지금처럼 글자 표를 기다리지 않고, 고를 것을 목록으로 주고 고르게 한다.
  화면 조작(TASK-KAR-235 다음 칸)이 서면 그 목록이 곧 **누를 것 목록**이 된다.
  지연 목표(1~3초)는 TASK-KAR-237 의 수용 기준에 그대로 쓴다.

### 2026-08-21 · 늦는 건 모델이 아니라 **부르는 방식**이었다 — 상주 두뇌

- **본 것**: 체감 지연을 지배하는 건 **첫 토큰까지의 시간(TTFT)**이고, 짧은 대화일수록 그렇다.
  로컬 소형(1B~3B)은 평범한 노트북에서 **10~20 tok/s** 로 「빠른 질문·요약·초안」에는 충분하다.
  재는 법도 정해져 있다 — 모델·양자화·러너 판본을 고정하고 5회 돌려 평균·표준편차·P50/P99 를
  같이 낸다(`llama-bench` 가 사실상 기준). 3B 미만은 CPU/GPU 양쪽을 다 재라 — 작은 모델은
  PCIe 전송이 GPU 이득을 먹는다.
  [지연 벤치 2026](https://www.digitalapplied.com/blog/ai-model-latency-benchmarks-2026-ttft-throughput) ·
  [로컬 셋업 재기](https://localaimaster.com/blog/benchmark-local-ai-setup) ·
  [llama.cpp 벤치](https://www.myaihardware.com/llama-cpp-benchmarks/)
- **우리와 다른 점**: 대조표의 「두뇌 = 로컬 상시 X」가 숫자로 확인됐다(102회차 실측).
  우리 TTFT 는 **9~14초**고, 그중 프로세스 켜는 데만 1.4~1.8초다. 결정적인 것은 **모델 탓이
  아니라는 것** — 같은 CLI 를 빈 폴더에서 「짧게 인사」 한 줄로 직접 불러도 **10.7초**다.
  그림을 붙이든 안 붙이든 차이가 없었다(A/B 2판: 그림 9.5·14.2초 / 그림 없이 13.3·9.1초).
  즉 늦는 건 haiku 도, 재료 크기도, 100회차의 그림도 아니고 **매 turn 새 프로세스를 띄워
  왕복하는 구조** 자체다.
- **할 수 있는 것**: 자리는 **이미 있고 죽어 있다.** `brain/assistant.ts` 가 `karmolab-ai` 의
  provider 라우터로 위임하고, 그 목록에 `ollama` 가 들어 있다(`ASSISTANT_AI_PROVIDER`).
  지금 `COMPANION_BRAIN=assistant` 로 띄우면 **대답이 없다** — 먼저 이걸 살리고, 상주 서버
  (llama.cpp/ollama)에 물린 다음 TTFT 를 5회 평균으로 재서 CLI 10.7초와 나란히 놓는다.
  인격·손·그림을 아직 안 다루는 것도 그 두뇌의 숙제다. → 판 TASK: **TASK-KAR-237**

### 2026-08-21 · 기억을 「글 한 덩이」가 아니라 **이름 붙은 칸**으로

- **본 것**: Letta 의 뼈대는 **memory block** 하나다 — **이름표가 붙은, 계속 남는 글 조각**.
  창(context)에 **항상 붙어 있고**, 쓰임새별로 나뉘며(`human` = 이 사람에 대해 아는 것 ·
  `persona` = 나는 누구인가 · 그 밖에 일·계획용 사용자 정의 칸), **에이전트가 스스로 고친다**
  (`core_memory_append` / `core_memory_replace`). 칸마다 DB 에 따로 저장되고 `block_id` 로
  집어 볼 수 있다. **읽기 전용으로 잠글 수도** 있다 — 그러면 사람만 고친다. 요청마다 창을
  DB 의 현재 칸 값으로 **다시 조립**한다. 이 구조는 MemGPT 때부터 이어진 것이다.
  [memory block 설명](https://www.letta.com/blog/memory-blocks/) ·
  [core memory 문서](https://docs.letta.com/guides/core-concepts/memory/memory-blocks) ·
  [구조 정리](https://deepwiki.com/letta-ai/letta/3-memory-system)
- **우리와 다른 점**: 우리 「아는 것」은 **글 한 덩이**다(`아는-것.md` 한 파일, 졸일 때마다
  통째로 새로 씀). 그래서 105회차에 **한 줄이 오염되자 통째로 못 믿을 것**이 됐고, 틀린 줄
  하나만 지울 방법이 없었다. 반대로 우리가 앞선 것도 뚜렷하다 — 저쪽은 **에이전트가 제 기억을
  고치는데**, 우리는 두뇌가 지어낸 것을 못 믿어 105회차에 담기 전 관문을 세웠다. 「고칠 수
  있게」와 「아무거나 못 넣게」는 같이 가야 한다.
- **할 수 있는 것**: TASK-KAR-236 2단계를 **칸 나누기**로 잡는다 — 「이 사람에 대해」와
  「나는 누구인가」를 **다른 칸**으로 두고(우리는 후자를 `characters/*.md` 로 이미 갖고 있다),
  각 칸을 줄 단위로 담는다. 105회차 관문(`memory/known-clean.ts`)은 **칸에 쓰기 전** 그대로
  선다. 사람이 지우는 길은 이미 있다(창의 「잊어줘」) — 칸이 나뉘면 **줄 하나만** 지울 수 있다.

### 2026-08-21 · 오래 남는 기억은 「담기 전」에 걸러진다 — 자기상 축

- **본 것**: 오래 가는 기억은 2026 에 재는 자리가 생겼다. **LoCoMo**(35세션·평균 300턴·
  1,540문항, 단일홉/다중홉/열린질문/시간추론)와 **LongMemEval**, 그리고 아직 아무 구조도
  포화 못 시킨 **BEAM**. 숫자: Mem0 는 LongMemEval **93.4%** · LoCoMo **91.6%** 를 **7,000토큰
  미만** 회수로 낸다(전체 맥락을 다 넣는 방식은 25,000+). Zep 은 LoCoMo **94.7%**.
  인격 유지 쪽은 따로 잰다 — O-Mem 이 **PERSONAMEM 62.99%**. 열린 물건 중 자리는 **Letta**
  (구 MemGPT, 13k+ stars) 가 잡고 있고, 기억을 API 가 아니라 **에이전트 상태의 일부**로 둔다.
  [기억 벤치 2026](https://mem0.ai/blog/ai-memory-benchmarks-in-2026) ·
  [에이전트 기억 현황 2026](https://mem0.ai/blog/state-of-ai-agent-memory-2026) ·
  [열린 기억 얼개 훑기](https://evermind.ai/blogs/best-open-source-agent-memory-frameworks-2026)
- **우리와 다른 점**: 우리 기억은 3겹 + 뜻 색인이라 **회수**는 서 있다(94회차에 0.94 로
  회상). 없는 것은 **재는 자리**와 **담기 전 관문**이다. 101회차에 실물이 나왔다 —
  「이 사람에 대해 아는 것」에 요약 두뇌의 메타 발언이 통째로 굳어 있었다(「이 대화는
  창작물이나 상황극으로 보여서 …추출하기 어렵습니다」). 무대 뒤 말 지킴이는 **말**은 보는데
  **기억에 담기는 것**은 안 본다. 대조표의 「기억 = 우리가 앞섬」은 회수 얘기지 자기상 얘기가
  아니다.
- **할 수 있는 것**: 지킴이(`meta-talk`·`hollow`·`garbled`)를 **입 앞에만** 두지 말고
  `memory/졸이는 기억`이 「아는 것」을 쓰기 **전에** 한 번 더 태운다. 더해서 한 덩이 글이 아니라
  **사실 한 줄씩**(언제 들었나 · 누가 말했나)으로 담으면 틀린 줄 하나만 지울 수 있다.
  재는 자리는 LoCoMo 를 그대로 못 쓰니(한국어·우리 대화) 우리 대화 로그로 작은 판을 뜬다.
  → 판 TASK: **TASK-KAR-236**

### 2026-08-21 · 화면을 「그림만」 보는 건 이제 옛 방식 — 하이브리드 인식 (접근성 트리 + 그림)

- **본 것**: 2026 현재 컴퓨터 조작 에이전트의 합의는 **하이브리드**다 — 구조가 있는 것은
  접근성 트리(윈도우 = UI Automation, 화면 낭독기가 쓰는 그 API)로 읽고, 캔버스·그림처럼
  구조가 없는 것만 그림으로 본다. 숫자로도 갈린다: Browser-Use(하이브리드) WebVoyager
  **89.1%** vs Agent-E(접근성만) **73.1%**. 그림만 쓰는 쪽은 「작은 글자를 못 읽는다」가
  고질이라 CogAgent 는 입력 해상도를 **1120x1120** 까지 올려서 버틴다. 열린 물건 중
  GUI-Owl/Mobile-Agent-v3 는 OSWorld **37.7%** · AndroidWorld **73.3%** (코드·가중치 공개).
  사람은 OSWorld 에서 72.4%, 그림만 보는 초기 에이전트는 12% 대였다.
  흔한 구현은 트리를 훑어 **조작 가능한 것마다 번호를 붙인 그림**(Set-of-Marks)을 만들고,
  좌표 대신 그 번호로 누른다(pywinauto 로 트리 추출).
  [Zylos 2026 정리](https://zylos.ai/research/2026-02-08-computer-use-gui-agents/) ·
  [OSWorld-G](https://osworld-grounding.github.io/) ·
  [열린 구현 훑기](https://fazm.ai/blog/best-open-source-computer-use-agent-windows-2026)
- **우리와 다른 점**: 우리 눈은 **그림 한 장뿐**이다(`sense/screen.ts` → `capture-screen.ps1`).
  글자로 오는 건 창 제목 하나(`TITLE=`). 대조표의 「화면 = 보기만, 조작 X」가 여기서 온다.
  좌표를 줄 수 있는 구조가 없으니 조작으로 넘어갈 길도 지금은 없다.
  99회차에 그림을 1097px → 1568px 로 키웠지만, 그건 **같은 방식을 더 크게** 한 것이다.
- **할 수 있는 것**: `capture-screen.ps1` 이 이미 `user32.dll` 을 부르고 있다 — 같은 자리에
  UIAutomation(`System.Windows.Automation`)으로 **전면 창의 요소 트리**(이름·종류·좌표)를
  뽑아 `TITLE=` 옆에 한 줄로 같이 내보낸다. `screenSense` 는 그걸 `meta.uiTree` 로 싣고,
  `ThinkInput.seeing` 과 나란히 두뇌 앞에 놓는다(99회차에 낸 자리를 그대로 쓴다).
  이게 서면 (1) 작은 글자를 그림으로 못 읽어도 **글자로** 읽고, (2) 요소마다 좌표가 생겨
  다음 단계인 **조작**의 재료가 된다. → 판 TASK: **TASK-KAR-235**

### 씨앗 (아직 안 판 것들 — 여기서 시작)

- 뉴로사마 — 말 끊기 · 먼저 말하기 · 오래 가는 기억 · 손을 상시로 씀 (2026-08-21 항목에서 판다)
